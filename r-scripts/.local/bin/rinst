#!/usr/bin/env Rscript
args <- commandArgs(trailingOnly = TRUE)
if (length(args) == 0) {
    args <- "."
}

devtools::document(args)
devtools::install(args, dependencies = TRUE)

# vim: set filetype=r:
